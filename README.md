# 📝 Spring Boot Blog

A lightweight **Java + Spring Boot** backend for a simple blog system.  
Built with Spring MVC, JPA/Hibernate, and Maven.

## 🚀 Features
- Create, read, update, and delete blog posts (CRUD)
- MVC architecture
- JPA/Hibernate ORM
- H2 in-memory database (MySQL optional)
- Includes Maven Wrapper for easy builds

## 📁 Project Structure
